{
  "name": "@onivoro/server-elastic-search",
  "version": "0.1.0",
  "repository": {
    "url": "https://github.com/onivoro/server-elastic-search.git"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "release": "rm -rf dist && npm run build && npm version minor && cp package.json dist && cd dist && npm publish --scope public"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "rxjs": "^6.6.3",
    "@onivoro/server-process": "latest"
  }
}
